# A Moment of Light

An interactive poem generator: each visit (or click of the refresh button) shows a random short poem with its author, drawn from a client-side collection (`poems.json`). The content is generated dynamically in the browser and has no fixed text to render here — visit the live page at https://paulochang.com/quotes/ to read one.
